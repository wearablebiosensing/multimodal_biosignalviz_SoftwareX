# Start from the official scientific python image
FROM quay.io/jupyter/scipy-notebook:latest

# Update pip and install required libraries (added plotly)
RUN pip install --no-cache-dir --upgrade \
    pip \
    numpy \
    pandas \
    plotly